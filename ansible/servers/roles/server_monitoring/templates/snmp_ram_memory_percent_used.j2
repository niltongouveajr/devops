#!/bin/bash

RAM_MEMORY_TOTAL=`free | grep Mem | awk -F " "  '{print $2}'`
RAM_MEMORY_USED=`free | grep Mem | awk -F " "  '{print $3}' | sed -e 's|$|00|g'`
RAM_MEMORY_PERCENT_USED=$(($RAM_MEMORY_USED/$RAM_MEMORY_TOTAL))

echo "${RAM_MEMORY_PERCENT_USED}"
