---

# Tasks file to "fix_ssh_diffie_hellmann"

 - name: "fix_ssh_diffie_hellmann: Copy from template config to {{DEVOPS_SSH_PATH}}"
   copy:
     src=files/config
     dest={{DEVOPS_SSH_PATH}}
     mode=0700
