---

# Tasks file to "clean_packages"

 - name: "clean_packages: Run apt-get update"
   apt: update_cache=yes

 - name: "clean_packages: Run apt-get autoclean"
   apt: autoclean=yes

 - name: "clean_packages: Run apt-get autoremove"
   apt: autoremove=yes
